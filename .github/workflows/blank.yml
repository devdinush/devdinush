on:
  workflow_dispatch:
  schedule:
  - cron: "0 0 * * *" # triggers every midnight
jobs:
  update-readme-with-blog:
    name: Update this repo's README with Sinhala Quote
    runs-on: ubuntu-latest
    steps:
      - name: Sinhala Quote Of The Day
        uses: dinushchathurya/sinhala-quotes-github-action@1.0.0


